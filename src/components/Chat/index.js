import { Box } from '@mui/material'

const FRONTEND_URL = process.env.REACT_APP_FRONTEND_URL

const BACKEND_URL = process.env.REACT_APP_BACKEND_URL

const Chat = () => {
  return <Box>Here is chat, welcome!</Box>
}
export default Chat
